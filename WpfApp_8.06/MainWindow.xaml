<Window x:Class="WpfApp_8._06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_8._06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Number of Progress Bars:" VerticalAlignment="Center"/>
            <TextBox x:Name="NumberOfBarsTextBox" Width="50" Margin="5"/>
            <Button Content="Create Bars" Click="CreateBars_Click" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Start Dancing" Click="StartDancing_Click" Margin="5"/>
            <Button Content="Stop Dancing" Click="StopDancing_Click" Margin="5"/>
        </StackPanel>

        <UniformGrid Grid.Row="2" x:Name="ProgressBarContainer" Columns="3"/>
    </Grid>
</Window>
