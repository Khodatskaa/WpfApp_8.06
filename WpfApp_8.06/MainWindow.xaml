<Window x:Class="WpfApp_8._06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_8._06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Start Race" Click="StartRace_Click" HorizontalAlignment="Center" Margin="10"/>

        <DataGrid x:Name="ResultsDataGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Horse" Binding="{Binding HorseName}"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Position}"/>
            </DataGrid.Columns>
        </DataGrid>

        <UniformGrid Grid.Row="2" x:Name="ProgressBarContainer" Columns="1"/>
    </Grid>
</Window>
